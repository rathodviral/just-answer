import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import App from "./App";
import reportWebVitals from "./reportWebVitals";
import { Provider } from "react-redux";
import store from "./store";

ReactDOM.render(
  <Provider store={store}>
    <App />
  </Provider>,
  document.getElementById("root")
);

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals
reportWebVitals();
const rem = window.atob("MC41cmVt");
const devInfoSec = document.createElement("div");
devInfoSec.textContent = window.atob("RGV2ZWxvcGVkIEJ5IDogVmlyYWwgUmF0aG9k");
devInfoSec.style.textAlign = window.atob("Y2VudGVy");
devInfoSec.style.marginTop = rem;
devInfoSec.style.marginBottom = rem;
devInfoSec.style.padding = rem;
document.getElementById("root").append(devInfoSec);
